{
  "name": "SOOQ",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "web:build": "pnpm --filter web build",
    "web:dev": "pnpm --filter web dev --webpack",
    "web:start": "pnpm --filter web start",
    "store:build": "pnpm --filter store build",
    "store:dev": "pnpm --filter store dev",
    "store:start": "pnpm --filter store start --port 3000"
  },
  "devDependencies": {
    "@workspace/eslint-config": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "turbo": "^2.8.17",
    "typescript": "5.9.3"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@radix-ui/react-direction": "^1.1.1",
    "@tanstack/react-query": "^5.99.0",
    "axios": "^1.15.0",
    "js-cookie": "^3.0.5",
    "ui": "^0.2.4",
    "web": "^0.0.2"
  }
}
