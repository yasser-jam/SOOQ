import { LandingPage } from "@/components/landing/landing-page"

/**
 * Interim marketing landing. Will move to a separate domain later.
 * Merchant dashboard home is `/` (auth-gated).
 */
export default function WelcomePage() {
  return <LandingPage />
}
